# Active project heartbeat

- Continue the AI operations control-plane project layer by layer without waiting for reminders.
- Keep the dashboard activity state, notes, tasks, and progress surfaces updated when meaningful work is done.
- If there is a real update, blocker, decision, or approval/DNS need, send a concise progress update instead of HEARTBEAT_OK.
- If there is no meaningful change worth sending, reply HEARTBEAT_OK.
